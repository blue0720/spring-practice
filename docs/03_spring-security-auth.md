# 03. Spring Securityによる会員機能

対応フェーズ: フェーズ2

## 概要
セッションベースのフォームログイン認証を実装する。暫定の全許可 `SecurityConfig`(チケット01)を正式な設定に置き換える。

## Todo
- [ ] ユーザー登録機能(`GET/POST /register`): ユーザー名・メールアドレス・パスワード
- [ ] パスワードをBCryptでハッシュ化して保存
- [ ] ログイン機能(`GET /login`、フォームログイン)
- [ ] ログアウト機能
- [ ] ロール定義: `ROLE_PLAYER`(一般プレイヤー) / `ROLE_ADMIN`(コンテンツ管理者)
- [ ] `/game` と `/api/**` を未ログイン時アクセス不可にし、`/login` にリダイレクト
- [ ] `/admin/**` を `ROLE_ADMIN` のみアクセス可能に制限(チケット06と連携)
- [ ] `GET /` をログイン状態に応じて `/game` または `/login` にリダイレクト
- [ ] CSRF対策を有効化(暫定で無効化していたものを復元)
- [ ] `spring-boot-starter-validation` で会員登録フォームのサーバーサイドバリデーション実装
- [ ] チケット01の暫定 `SecurityConfig`(`permitAll`)を削除・置き換え

## 参照
- `02_要件定義書.md` セクション3.1「会員機能」、セクション2「認証方式」

## メモ
- Thymeleafとの相性を優先し、JWTではなくセッションベース認証を採用する方針(要件定義書で確定済み)。
